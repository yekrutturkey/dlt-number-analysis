# AGENTS.md

本文件适用于仓库根目录及全部子目录。

## 安全约束

- 禁止批量删除文件或目录。
- 不得使用 `del /s`、`rd /s`、`rmdir /s`、`Remove-Item -Recurse` 或 `rm -rf`。
- 如确需删除，只能一次删除一个已经确认的明确文件路径，例如：
  `Remove-Item "C:\path\to\file.txt"`。
- 如果任务要求批量删除文件，停止操作并请用户手动处理。
- 不得覆盖或回滚与当前任务无关的用户改动。

## 工程约束

- 使用 Python 3.12+、`uv`、`pytest` 和 `ruff`。
- 所有核心函数必须有完整类型注解；业务逻辑按职责拆分，禁止集中到单一文件。
- 数据抓取、数据校验、特征分析、策略评分、回测与结果输出保持清晰边界。
- 外部抓取结果必须先经过校验，分析层不得直接依赖网络或网页结构。
- 修改后执行 `uv run ruff format .`、`uv run ruff check .` 和 `uv run pytest`。

## GitHub 自动同步

- 用户已授权：每完成一个边界清晰、可以独立验证的项目阶段，在质量检查通过后自动提交并推送到已配置的 GitHub 私有仓库。
- 只暂存本阶段范围内的文件；如发现来源不明或与本阶段无关的改动，停止自动提交并向用户确认。
- 不得提交密码、访问令牌、Cookie、私钥、个人敏感数据或本地环境文件。
- 首次基线保存在 `main`；后续较大改动使用 `codex/<简短描述>` 分支并优先创建草稿 PR，小型且明确的维护改动可以推送当前已授权分支。
- 如果测试失败、GitHub 登录失效、远程仓库不可访问或推送产生冲突，不得强行上传；应保留本地改动并报告原因。

## 数据与回测约束

- 历史 CSV 格式以 README 和 `dlt_number_analysis.data.data_validator` 中的
  `CSV_COLUMNS` 为准。
- 不得补造、猜测或静默修正历史开奖数据；发现异常时应明确报错。
- 历史预测在任一时间点只能使用当时已开奖且已经校验的数据，禁止未来数据泄漏。
- 时间序列回测必须使用向前滚动切分，不能使用普通随机训练/测试拆分。
- 每次预测必须记录模型版本、数据截止期号、带时区的生成时间和完整参数。
- 所有评分、候选号码、预测、回测和报告输出必须包含：
  “评分不等于真实中奖概率，彩票开奖结果是随机事件。”
- 不得将统计评分描述为中奖概率，也不得声称能够预测中奖号码。
